# homebrew-rc0

Homebrew tap for [`rc0`](https://github.com/zoltanf/rc0-cli) — a command-line tool for the [RcodeZero Anycast DNS API](https://www.rcodezero.at).

## Install

```sh
brew install zoltanf/rc0/rc0
```

## Upgrade

```sh
brew upgrade rc0
```
